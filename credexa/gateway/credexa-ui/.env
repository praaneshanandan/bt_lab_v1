# During development (localhost:5173), point API calls to Gateway running on 8080
VITE_API_BASE_URL=http://localhost:8080

# For production build (served from Gateway), use empty string for relative URLs
# This allows the same build to work with both localhost:8080 and ngrok URLs
